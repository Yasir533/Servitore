<Window x:Class="Servitore.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Servitore - Login" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="40">
        <StackPanel VerticalAlignment="Center">

            <!-- Logo / Title -->
            <TextBlock Text="Servitore" FontSize="34" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,4" />
            <TextBlock Text="Service Management System" FontSize="13" Opacity="0.6"
                       HorizontalAlignment="Center" Margin="0,0,0,36" />

            <!-- Connection Check State -->
            <StackPanel x:Name="ConnectionCheckPanel" Visibility="Visible">
                <TextBlock x:Name="ConnectionStatusText" Text="Starting Servitore..." FontSize="16" FontWeight="Medium" HorizontalAlignment="Center" Margin="0,20,0,24" />
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True"
                             HorizontalAlignment="Center"
                             Height="40" Width="40" />
            </StackPanel>

            <!-- Connection Failed State -->
            <StackPanel x:Name="ConnectionFailedPanel" Visibility="Collapsed" Margin="0,10,0,10">
                <TextBlock Text="Unable to connect to the server. Please ensure the Servitore API is running."
                           Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           FontSize="14"
                           Margin="0,0,0,32" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="RetryButton" Content="Retry" Click="RetryButton_Click" Width="100" Margin="0,0,16,0" Style="{StaticResource MaterialDesignRaisedButton}" />
                    <Button x:Name="ExitButton" Content="Exit" Click="ExitButton_Click" Width="100" Style="{StaticResource MaterialDesignFlatButton}" />
                </StackPanel>
            </StackPanel>

            <!-- Login Form Panel -->
            <StackPanel x:Name="LoginFormPanel" Visibility="Collapsed">
                <!-- Username -->
                <TextBox x:Name="UsernameBox"
                         materialDesign:HintAssist.Hint="Username"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,16"
                         IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}" />

                <!-- Password (Enter key triggers login) -->
                <PasswordBox x:Name="PasswordBox"
                             materialDesign:HintAssist.Hint="Password"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             KeyDown="PasswordBox_KeyDown"
                             Margin="0,0,0,8"
                             IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}" />

                <!-- Error message -->
                <TextBlock x:Name="ErrorText"
                           Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                           TextWrapping="Wrap"
                           Margin="0,4,0,8"
                           Visibility="Collapsed" />

                <!-- Login button with progress indicator -->
                <Button Content="LOG IN"
                        Click="LoginButton_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,16,0,0"
                        Height="44"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}" />

                <!-- Busy spinner -->
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True"
                             HorizontalAlignment="Center"
                             Margin="0,16,0,0"
                             Height="28" Width="28"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
