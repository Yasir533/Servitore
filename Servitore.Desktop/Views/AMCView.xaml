<UserControl x:Class="Servitore.Desktop.Views.AMCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="AMC Management" FontSize="26" FontWeight="SemiBold" />
                <TextBlock Text="Annual Maintenance Contracts" Opacity="0.55" FontSize="13" />
            </StackPanel>
            <Button Content="+ Add Contract"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding AddContractCommand}"
                    HorizontalAlignment="Right" Margin="16,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <ProgressBar Grid.Row="1" Style="{StaticResource MaterialDesignLinearProgressBar}"
                     IsIndeterminate="True" Margin="0,0,0,8"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- Summary chips -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <Border Background="#E8F5E9" CornerRadius="16" Padding="14,6" Margin="0,0,8,0">
                <TextBlock Foreground="#2E7D32">
                    <Run Text="Active: " /><Run Text="{Binding ActiveCount}" FontWeight="Bold" />
                </TextBlock>
            </Border>
            <Border Background="#FFF9C4" CornerRadius="16" Padding="14,6" Margin="0,0,8,0">
                <TextBlock Foreground="#F57F17">
                    <Run Text="Expiring 30d: " /><Run Text="{Binding ExpiringCount}" FontWeight="Bold" />
                </TextBlock>
            </Border>
            <Border Background="#E3F2FD" CornerRadius="16" Padding="14,6">
                <TextBlock Foreground="#1565C0">
                    <Run Text="Total Value: ₹" /><Run Text="{Binding TotalValue, StringFormat='{}{0:N0}'}" FontWeight="Bold" />
                </TextBlock>
            </Border>
        </StackPanel>

        <TextBox Grid.Row="3"
                 materialDesign:HintAssist.Hint="Search by customer or contract number…"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12" />

        <Border Grid.Row="4" Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="6">
            <DataGrid ItemsSource="{Binding ContractsView}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserSortColumns="True"
                      materialDesign:DataGridAssist.CellPadding="14 8 14 8"
                      materialDesign:DataGridAssist.ColumnHeaderPadding="14 8 14 8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Asset"        Binding="{Binding AssetName}"     Width="*" />
                    <DataGridTextColumn Header="Customer"     Binding="{Binding CustomerName}"  Width="180" />
                    <DataGridTextColumn Header="Start Date"   Binding="{Binding StartDate, StringFormat='{}{0:dd MMM yyyy}'}" Width="110" />
                    <DataGridTextColumn Header="End Date"     Binding="{Binding EndDate, StringFormat='{}{0:dd MMM yyyy}'}"   Width="110" />
                    <DataGridTextColumn Header="Value (₹)"   Binding="{Binding ContractValue, StringFormat='{}{0:N0}'}"      Width="110" />
                    <DataGridTextColumn Header="Visits"       Binding="{Binding VisitsIncluded}" Width="70" />
                    <DataGridTextColumn Header="Days Left"    Binding="{Binding DaysRemaining}"  Width="90" />
                    <DataGridTextColumn Header="Status"       Binding="{Binding Status}"         Width="110" />
                    <DataGridTemplateColumn Header="Actions"  Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.EditContractCommand,
                                                      RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" ToolTip="Edit">
                                        <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16" />
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.DeleteContractCommand,
                                                      RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" ToolTip="Delete"
                                            Foreground="{DynamicResource MaterialDesignValidationErrorBrush}">
                                        <materialDesign:PackIcon Kind="Delete" Width="16" Height="16" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
