<UserControl x:Class="Servitore.Desktop.Views.ServiceEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Service Entries" FontSize="26" FontWeight="SemiBold" />
                <TextBlock Text="Track and manage service requests" Opacity="0.55" FontSize="13" />
            </StackPanel>
            <Button Content="+ New Entry"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding NewServiceEntryCommand}"
                    HorizontalAlignment="Right" VerticalAlignment="Center" DockPanel.Dock="Right" />
        </DockPanel>

        <ProgressBar Grid.Row="1" Style="{StaticResource MaterialDesignLinearProgressBar}"
                      IsIndeterminate="True" Margin="0,0,0,8"
                      Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- Filter row -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="160" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                      materialDesign:HintAssist.Hint="Search by entry number or customer…"
                      Style="{StaticResource MaterialDesignOutlinedTextBox}"
                      Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                      Margin="0,0,12,0" />
            <ComboBox Grid.Column="1"
                       materialDesign:HintAssist.Hint="Status filter"
                       Style="{StaticResource MaterialDesignOutlinedComboBox}"
                       ItemsSource="{Binding StatusFilters}"
                       SelectedItem="{Binding SelectedStatusFilter}" />
        </Grid>

        <!-- Data grid -->
        <Border Grid.Row="3" Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="6">
            <DataGrid ItemsSource="{Binding ServiceEntriesView}"
                       AutoGenerateColumns="False" IsReadOnly="True" CanUserSortColumns="True"
                       FontSize="14" RowHeight="44"
                       materialDesign:DataGridAssist.CellPadding="16 10 16 10"
                       materialDesign:DataGridAssist.ColumnHeaderPadding="16 10 16 10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Entry #"     Binding="{Binding ServiceEntryNumber}" Width="120" />
                    <DataGridTextColumn Header="Customer"    Binding="{Binding CustomerName}"       Width="180" />
                    <DataGridTextColumn Header="Product"     Binding="{Binding AssetName}"          Width="150" />
                    <DataGridTemplateColumn Header="Problem" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ProblemDescription}" TextTrimming="CharacterEllipsis" ToolTip="{Binding ProblemDescription}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Priority"    Binding="{Binding Priority}"           Width="90" />
                    <DataGridTextColumn Header="Status"      Binding="{Binding Status}"             Width="100" />
                    <DataGridTextColumn Header="Created"     Binding="{Binding CreatedDate, StringFormat='{}{0:dd MMM yyyy}'}" Width="110" />
                    <DataGridTemplateColumn Header="Actions" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                             Command="{Binding DataContext.ViewServiceEntryCommand,
                                                       RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                             CommandParameter="{Binding}"
                                             Width="32" Height="32" Padding="0" Margin="4,0"
                                             ToolTip="View / Edit">
                                        <materialDesign:PackIcon Kind="Eye" Width="16" Height="16" />
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                             Command="{Binding DataContext.CloseServiceEntryCommand,
                                                       RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                             CommandParameter="{Binding}"
                                             Width="32" Height="32" Padding="0" Margin="4,0"
                                             ToolTip="Close Entry">
                                        <materialDesign:PackIcon Kind="CheckCircle" Width="16" Height="16" />
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                             Command="{Binding DataContext.DeleteServiceEntryCommand,
                                                       RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                             CommandParameter="{Binding}"
                                             Width="32" Height="32" Padding="0" Margin="4,0"
                                             ToolTip="Delete" Foreground="{DynamicResource MaterialDesignValidationErrorBrush}">
                                        <materialDesign:PackIcon Kind="Delete" Width="16" Height="16" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
